PORT=3000
ETH_NODE_WS=wss://<URL>.com/<VERSION>/<API_KEY> # Alchemy
GRACE_PERIOD_MS=30000 # Startup grace period before health check fails (default: 20000ms)
GAS_PRICE_STALENESS_MS=20000 # How long before cached gas data is considered stale (default: 30000ms)
